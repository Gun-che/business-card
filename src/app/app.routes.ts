import { Routes } from '@angular/router';
import { BusinessCardComponent } from 'src/app/components/business-card/business-card.component';

export const routes: Routes = [{ path: '', component: BusinessCardComponent }];
